spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/pinapp-clients-db
    username: pinapp
    password: pinapp_test
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
